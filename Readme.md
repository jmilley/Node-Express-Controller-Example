# Express Example App

1. `gh repo clone johnmilley/Node-Express-Controller-Example`
2. `cd Node-Express-Controller-Example`
3. `npm install`
4. `npm run dev` or `node index.js`
5. Open `localhost:3000` in browser.